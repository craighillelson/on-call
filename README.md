Python 2.7 script to loop through a list of employees to assign them on call shifts that start on Mondays.

Instructions
Save script to a directory containing a csv of employees.
In Terminal, type "python on-call.py"
